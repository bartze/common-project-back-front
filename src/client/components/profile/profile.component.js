const Profile = () => {
	return (
		<div className="home-content">
			<h1>Profile</h1>

			<button
				onClick={() => alert('Suerte...')}
				className="add-project-btn"
			>
				Add Profile
			</button>
		</div>
	);
};

export default Profile;
