const Games = () => {
	return (
		<div className="home-content">
			<h1>Games</h1>

			<button
				onClick={() => alert('Suerte...')}
				className="add-project-btn"
			>
				Add Game
			</button>
		</div>
	);
};

export default Games;
